<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:popupMenuStyle">@style/popup_menu_style</item>
        <item name="android:fontFamily">Arial</item>
    </style>

    <style name="popup_menu_style" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/basic_blue</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="android:background">@color/basic_blue</item>
    </style>

</resources>
